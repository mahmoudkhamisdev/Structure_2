import { FileNode, Project } from "./types";

export const initialFileTree: FileNode[] = [];

export const initialProjects: Project[] = [
  { id: "1", name: "Default Project", plan: "Free", files: [] },
];
